{
  "dependencies": {
    "cross-spawn": "^7.0.3",
    "hello-friend": "github:panr/hugo-theme-hello-friend#3.0.0",
    "hugo-extended": "^0.121.1",
    "patch-package": "^8.0.0",
    "prettier": "^3.1.1"
  },
  "scripts": {
    "format": "prettier --write content/",
    "format:check": "prettier --check content/",
    "build": "node build.js",
    "server": "hugo server",
    "make-patch": "patch-package hello-friend",
    "postinstall": "patch-package"
  }
}
