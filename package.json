{
  "dependencies": {
    "hugo-extended": "^0.100.1",
    "prettier": "^2.6.2"
  },
  "scripts": {
    "format": "prettier --write content/",
    "format:check": "prettier --check content/",
    "server": "hugo server"
  }
}
